# AllocationAppeal

Scores an allocation under a frozen rubric and preserves an appeal window after scoring. The subject alone may appeal before its deadline; after that deadline any caller can resolve the case.

Run `PYTHONUTF8=1 genvm-lint contracts/contract.py` and `python -m pytest -q`.
